#pragma once

#include <string>
#include <filesystem>

namespace varan {
namespace neural {
namespace constants {
		
	constexpr int GATEWAY_DETECTION_FONT_HEIGHT = 30; // подпись класса на боксе
    constexpr int GATEWAY_OVERLAY_FONT_HEIGHT = 36;   // блок времени/GPS

	// Пути переехали в varan::paths().neural — см. include/core/paths.h.

} // constants
} // neural
} // varan