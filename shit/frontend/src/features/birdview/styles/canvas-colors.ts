/**
 * Цвета для отрисовки на холстах.
 *
 * Канвас не знает про CSS-переменные: в него уходят готовые строки. Пока они
 * лежали по месту использования, смена акцента в теме оставляла холсты
 * конфигуратора и проекции со старым цветом. Здесь они собраны в одном месте
 * и повторяют значения токенов --bv-* из theme.css.
 */

export const CANVAS_COLORS = {
    accent: '#c8ff40',
    accentDim: '#8aaf20',
    /** Заливка выделенной области. */
    accentFill: 'rgba(200, 255, 64, 0.10)',
    /** Направляющие и вспомогательные линии поверх содержимого. */
    accentGuide: 'rgba(200, 255, 64, 0.30)',
    /** Ломаная между точками разметки. */
    accentLine: 'rgba(200, 255, 64, 0.75)',

    /** Обводка точек и подложка под подписями — цвет фона страницы. */
    base: '#121216',
    /** Плашка под подписью на поле конфигуратора — цвет панели. */
    panelBackdrop: 'rgba(35, 35, 44, 0.90)',
    shadow: 'rgba(0, 0, 0, 0.55)',
    labelBackdrop: 'rgba(0, 0, 0, 0.70)',
} as const;
